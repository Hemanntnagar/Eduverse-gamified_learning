# Optional — only needed when the backend is on a different host (e.g. Vercel + Railway).
# Local dev works without this file: requests go to /api and Vite proxies to localhost:5000.
# VITE_API_URL=https://your-backend-url.com/api
